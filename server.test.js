const { handleRequest } = require(".");

describe('handleRequest', () => {
  it('should respond with status 200 and "Updated \\n"', () => {
    // Mock req and res
    const a = 1

    expect(a).toBe(1);
  });
});
